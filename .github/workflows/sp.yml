name: my first workflow | shell script
on:
  push:
    branches:
      -main

jobs:
  sprints_job:
    runs_on: ubuntu-latest
    steps:
      -name: list files
      -run: ./script.sh

  basmala_job:
    runs_on: windows
    steps:
      -name: list files
      -run: echo "i am basmala"